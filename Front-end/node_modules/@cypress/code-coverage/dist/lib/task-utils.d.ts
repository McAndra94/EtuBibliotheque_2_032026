import { type NycOptions } from './common-utils';
export interface CoverageEntry {
    path: string;
    hash?: string;
    [key: string]: unknown;
}
export interface CoverageMap {
    [key: string]: CoverageEntry;
}
export declare function readNycOptions(workingDirectory: string): NycOptions;
export declare function checkAllPathsNotFound(nycFilename: string): boolean | undefined;
/**
 * A small debug utility to inspect paths saved in NYC output JSON file
 */
export declare function showNycInfo(nycFilename: string): void;
/**
 * Looks at all coverage objects in the given JSON coverage file
 * and if the file is relative, and exists, changes its path to
 * be absolute.
 */
export declare function resolveRelativePaths(nycFilename: string): void;
/**
 * @param filepaths
 * @returns common prefix that corresponds to current folder
 */
export declare function findCommonRoot(filepaths: string[]): string | undefined;
export declare function tryFindingLocalFiles(nycFilename: string): void;
/**
 * If the website or unit tests did not load ALL files we need to
 * include, then we should include the missing files ourselves
 * before generating the report.
 *
 * @see https://github.com/cypress-io/code-coverage/issues/207
 */
export declare function includeAllFiles(nycFilename: string, nycOptions: NycOptions): void;
//# sourceMappingURL=task-utils.d.ts.map