/**
 * Registers code coverage collection and reporting tasks.
 * Sets an expose variable to tell the browser code that it can
 * send the coverage.
 * @example
  ```
    // your plugins file
    module.exports = (on, config) => {
      require('cypress/code-coverage/task')(on, config)
      // IMPORTANT to return the config object with any changes
      return config
    }
  ```
*/
declare const _default: (on: Cypress.PluginEvents, config: Cypress.PluginConfigOptions) => Cypress.PluginConfigOptions;
export = _default;
//# sourceMappingURL=task.d.ts.map